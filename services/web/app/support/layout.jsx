export const metadata = {
  title: 'Support Small Things — Terry Heath',
  description: 'Support independent writing. Your contribution helps keep Small Things going.',
};

export default function SupportLayout({ children }) {
  return children;
}